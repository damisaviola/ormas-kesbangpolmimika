# Git Workflow Rule

Do NOT automatically perform `git push` to remote repositories (GitHub).
Keep all changes local, verify them with `npm run build`, and only push when explicitly instructed by the user.
